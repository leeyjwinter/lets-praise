pages:
  stage: deploy
  script:
    - mkdir public
    - cp index.html public/
    - cp -r css public/
    - cp -r js public/
    - cp -r data public/
  artifacts:
    paths:
      - public
  only:
    - main
